<h1>Edit Products</h1>

<%#If you give the form_for helper an ActiveRecord object that is persisted then %>
<%#the generated form will have an action that submits to 'product_path(@product)'%>
<%# where @product is the ActiveRecord object htat you passed to the form %>
<%= render 'form' %>
